# Core dependencies (pinned for reproducibility)
pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
pyyaml>=6.0.0
requests>=2.31.0
pyarrow>=14.0.0
# torch>=2.0.0
scikit-learn>=1.3.0
click>=8.1.0
apscheduler>=3.10.0
flask>=3.0.0
pytest>=7.4.0

# Note: PyTorch removed to save space. To reinstall CPU-only version (small):
# pip install torch --index-url https://download.pytorch.org/whl/cpu

# Optional performance optimizations
# TA-Lib is optional and recommended if you can install its native deps
# ta-lib
pandas_ta>=0.3.14

