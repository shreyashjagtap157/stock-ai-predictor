version: '3.8'

services:
  predictor:
    build: .
    volumes:
      - ./data:/app/data
      - ./config.yaml:/app/config.yaml
    environment:
      - PYTHONUNBUFFERED=1
    command: ["python", "src/cli/cli.py", "--train"]
